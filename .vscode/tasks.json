{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "cmake --build ${workspaceFolder}/bld",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/bld"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "type": "shell",
            "label": "UT_build",
            "command": "cmake --build ${workspaceFolder}/UnitTests/bld",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}/UnitTests/bld"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}